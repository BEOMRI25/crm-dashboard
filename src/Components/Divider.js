import './Divider.css'

export default function Divider() {
  return <div className='divider'></div>
}

// background color of quadruple positive layer
// a line with a thickness of 1.5 px, whether it's horizontal or vertical, and it's size on the other axis fills the available space
// maximum border radius
